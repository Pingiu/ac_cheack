name: Use Create File

on:
  push:
    branches: [main]
    
jobs:
  call-reusable:
    uses: Pingiu/my_private/.github/workflows/reusable.yml@main
    with:
      some-input: value
    secrets:
      MY_SECRET: ${{ secrets.MY_SECRET }}

