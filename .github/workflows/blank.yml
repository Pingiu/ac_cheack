name: Action

on:
  workflow_dispatch:

jobs:
  run-private:
    runs-on: ubuntu-latest
    steps:


      - name: Checkout  action repo
        uses: actions/checkout@v4
        with:
          repository: ${{secrets.PRIVATE_ORG}}/${{ secrets.PRIVATE_REPO }}
          token: ${{ secrets.GITHUB_TOKEN }}
          path: ./${{ secrets.PRIVATE_REPO }}

      - name: Run secure update action
        uses: ./my_private/.github/workflows/my-reusable.yml
