name: Use Create File

on:
  push:
    branches: [main]

jobs:
  call-private-workflow:
    uses: Pingiu/my_private/.github/workflows/reusable.yml@main
    with:
      filename: secret.txt
      content: "Это секретное содержимое"
